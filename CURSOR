DECLARE
  P_X VARCHAR2(50);
  P_Y INTEGER;
CURSOR C IS SELECT X,Y
            FROM DUAL;
BEGIN
  OPEN C 
  LOOP
    FATCH C INTO P_X,P_Y;
    DBMS_OUTPUT.PUT_LINE(C.P_X);
  EXIT WHEN (C%ROWCOUNT < 5) OR C%NOTFOUND;
  END LOOP;
  
END;
