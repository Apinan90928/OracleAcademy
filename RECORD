DECLARE
  TYPE PRD_TYPE_RECORD IS RECORD
  ( PRD_ID NUMBER(5),
    PRD_NAME VARCHAR2(50)
  )
  PRD_TYPE PRD_TYPE_RECORD
BEGIN
  SELECT * INTO PRD_TYPE
  FROM PRODUCT
  WHERE PRD_CODE = 20;
  DBMS_OUTPUT.PUT_LINE(PRD_TYPE.PRD_NAME);
END;
